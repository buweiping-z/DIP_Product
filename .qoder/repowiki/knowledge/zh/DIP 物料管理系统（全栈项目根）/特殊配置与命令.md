后端 `cd dip-system/api && dotnet run`（端口 8800），前端 `cd dip-system/frontend-web && npm run dev`（端口 3000，Vite 代理到 8800），Swagger 地址 `http://localhost:8800/swagger`，自包含发布 `dotnet publish -c Release --self-contained -r win-x64`。